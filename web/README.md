# hivemind website

The repo-root `README.md` is the canonical source of truth for all website content. This site (`web/`) mirrors that content. There is no automated sync. Any content edit must trace back to a claim in `README.md`.
