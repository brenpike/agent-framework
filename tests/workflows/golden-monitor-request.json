{
  "workflow": "monitor-request",
  "description": "User request with watch/monitor keywords routes to the github-review-loop skill, which arms the main-session Monitor on its thin change-detect poll",
  "steps": [
    { "state": "Intake", "source": { "file": "plugin/agents/overlord.md", "pattern": "`watch`, `monitor`, `wait`, `poll`, or `loop`" } },
    { "state": "Monitor Arm", "source": { "file": "plugin/skills/github-review-loop/SKILL.md", "pattern": "skills/github-review-loop/scripts/pr-change-detect-poll.sh" } }
  ]
}
