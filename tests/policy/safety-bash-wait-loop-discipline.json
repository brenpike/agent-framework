{
  "rule": "Bash wait-loop discipline",
  "description": "P3 coupling: the Bash wait-loop / self-match discipline governance prose lives in plugin/governance/definitions.md (`background-task completion or Monitor is **the only sanctioned wait primitive**`) and is referenced by plugin/agents/overlord.md (`Bash Command Discipline per `). The source/consumers block below pins this rule coupling so that edits to the governance anchor or its consumer reference are caught. The source pattern carries the SUBJECT (background-task completion or Monitor) and not just the exclusivity predicate — rewriting WHICH primitives are the only sanctioned wait primitive must turn the suite red.",
  "source": { "file": "plugin/governance/definitions.md", "pattern": "background-task completion or Monitor is **the only sanctioned wait primitive**" },
  "consumers": [ { "file": "plugin/agents/overlord.md", "pattern": "Bash Command Discipline per " } ]
}
