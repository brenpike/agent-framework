{
  "rule": "Monitor truthfulness",
  "source": {
    "file": "plugin/agents/github-reviewer.md",
    "pattern": "If Monitor returns a non-error response and the first poll completes without a parser error, monitoring is active"
  },
  "consumers": [
    {
      "file": "plugin/agents/cerebrate.md",
      "pattern": "claim monitoring is active unless Monitor (or an equivalent real background trigger) returned a non-error response"
    }
  ]
}
