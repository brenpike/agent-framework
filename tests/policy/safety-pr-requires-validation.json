{
  "rule": "PR requires validation pass",
  "description": "P3 coupling, #337 re-pin: the open-plan-pr consumer pin carries the outcome discriminator — Blocked (`stage: validation`). WHICH report outcome forces exit 1 is the claim-carrying antecedent; without it, rekeying the exit on a different outcome left the suite green.",
  "source": {
    "file": "plugin/governance/workflow.md",
    "pattern": "Open PR only when: plan complete, validation passed, version bump included if required, branch pushed"
  },
  "consumers": [
    {
      "file": "plugin/agents/overlord.md",
      "pattern": "Run validation per `${CLAUDE_PLUGIN_ROOT}/governance/definitions.md` (Validation Procedure)"
    },
    {
      "file": "plugin/skills/open-plan-pr/SKILL.md",
      "pattern": "Exit 1 with blocker if the procedure returned the Worker Report — Blocked (`stage: validation`)"
    }
  ]
}
