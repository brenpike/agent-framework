{
  "rule": "react-marker.sh non-zero for non-thread candidate must fail closed to blocked marker-write-failed",
  "source": {
    "file": "plugin/agents/github-reviewer.md",
    "pattern": "if `react-marker.sh` exits NON-ZERO for a `toplevel` or `review` candidate"
  },
  "consumers": [
    {
      "file": "plugin/agents/github-reviewer.md",
      "pattern": "return `blocked` immediately with `blocker_reason: marker-write-failed`"
    },
    {
      "file": "plugin/agents/github-reviewer.md",
      "pattern": "this is the symmetric complement of the step-8 fail-closed clause"
    }
  ]
}
