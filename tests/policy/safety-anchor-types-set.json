{
  "rule": "Retrieval anchor types are exactly DEC, RISK, ASM, EVD (canonical Anchor ID Format lock)",
  "source": {
    "file": "plugin/governance/context-management-policy.md",
    "pattern": "### Anchor ID Format"
  },
  "set_check": {
    "extract_regex": "`([A-Z]{2,5})` \\([a-z]+\\)",
    "expected_set": ["DEC", "RISK", "ASM", "EVD"],
    "files": [
      { "path": "plugin/governance/context-management-policy.md", "mode": "equal" }
    ]
  }
}
