{
  "id": "cerebrate-missing-outcome",
  "version": 1,
  "start": "review_remediation_plan",
  "terminal": ["implement_next_step", "blocked"],
  "states": {
    "review_remediation_plan": {
      "type": "agent",
      "description": "Contract-class broken fixture: a cerebrate-agent state declaring {complete, blocked}. Satisfies NEITHER cerebrate output mode — missing open_questions for the ANALYSIS set, and missing single/multi/brood for the PLANNING set. Mirrors the original bug (review_remediation_plan was {complete, blocked}). Structurally valid (no dangling targets, all terminals declared) so ONLY the cerebrate-state contract check rejects it.",
      "agent": "hivemind:cerebrate",
      "transitions": {
        "complete": "implement_next_step",
        "blocked": "blocked"
      }
    },
    "implement_next_step": { "type": "terminal" },
    "blocked": { "type": "terminal" }
  }
}
