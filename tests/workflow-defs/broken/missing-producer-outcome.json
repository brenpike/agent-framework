{
  "id": "missing-producer-outcome",
  "version": 1,
  "start": "local_review",
  "terminal": ["open_pr", "review_remediation_plan", "review_exhausted", "user_input_required", "review_rejected", "blocked"],
  "states": {
    "local_review": {
      "type": "agent",
      "description": "Contract-class broken fixture: a reviewer state that OMITS the 'high-severity-rejection' producer outcome from local-reviewer's exit_reason vocabulary. Structurally valid (no dangling targets, all terminals declared) so ONLY the producer-to-workflow contract check rejects it.",
      "agent": "hivemind:local-reviewer",
      "transitions": {
        "clean": "open_pr",
        "planner-escalation": "review_remediation_plan",
        "max-iterations-reached": "review_exhausted",
        "break-fix-break": "review_exhausted",
        "diminishing-returns": "open_pr",
        "injection-suspect": "blocked",
        "user-input-required": "user_input_required",
        "blocked": "blocked"
      }
    },
    "open_pr": { "type": "terminal" },
    "review_remediation_plan": { "type": "terminal" },
    "review_exhausted": { "type": "terminal" },
    "user_input_required": { "type": "terminal" },
    "review_rejected": { "type": "terminal" },
    "blocked": { "type": "terminal" }
  }
}
