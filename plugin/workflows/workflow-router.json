{
  "id": "workflow-router",
  "version": 1,
  "start": "classify",
  "terminal": ["selected", "ambiguous", "exploratory", "blocked"],
  "states": {
    "classify": {
      "type": "skill",
      "description": "route-workflow classifies the request by judgment (never a keyword table) and selects which workflow handles it. Sole owner of 'which workflow'.",
      "skill": "hivemind:route-workflow",
      "transitions": {
        "selected": "initialize_ledger",
        "ambiguous": "confirm_workflow",
        "exploratory": "exploratory",
        "blocked": "blocked"
      }
    },
    "confirm_workflow": {
      "type": "user_gate",
      "description": "Ambiguous routes (2+ known candidates) require an explicit user choice before initializing the ledger.",
      "transitions": {
        "selected": "initialize_ledger",
        "cancelled": "blocked"
      }
    },
    "initialize_ledger": {
      "type": "skill",
      "description": "Create the run ledger for the chosen workflow before handing off to it.",
      "skill": "hivemind:init-run-ledger",
      "transitions": {
        "complete": "selected",
        "blocked": "blocked"
      }
    },
    "selected": { "type": "terminal" },
    "ambiguous": { "type": "terminal" },
    "exploratory": { "type": "terminal" },
    "blocked": { "type": "terminal" }
  }
}
